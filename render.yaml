services:
  # 1. Spring Boot Backend API
  - type: web
    name: comic-backend
    env: docker
    plan: free
    dockerContext: ./comic-backend-fixed
    dockerfilePath: ./comic-backend-fixed/Dockerfile
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: comic-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: comic-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: comic-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: comic-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: comic-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION
        value: "86400000"
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: APP_COOKIE_SECURE
        value: "true"
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: FRONTEND_URL
        value: "https://comic-frontend.onrender.com"

  # 2. React Frontend (Vite)
  - type: static
    name: comic-frontend
    plan: free
    buildCommand: cd comic-frontend-new && npm install && npm run build
    staticPublishPath: ./comic-frontend-new/dist
    envVars:
      - key: VITE_API_URL
        value: "https://mangaverse-25f5.onrender.com/api"
      - key: VITE_CLOUDINARY_CLOUD_NAME
        sync: false
      - key: VITE_CLOUDINARY_UPLOAD_PRESET
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  # Render Managed PostgreSQL (free tier — expires after 90 days)
  - name: comic-db
    databaseName: comic_db
    user: comic_user
    plan: free
